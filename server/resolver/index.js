
import mutations from './mutations'
import queries from './queries'
export { Upload, Date, Result } from './scalar'

export const Query = {
  ...queries
}

export const Mutation = {
  ...mutations
}
